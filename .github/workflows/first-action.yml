name: First workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      -name: Print greeeting
      run: echo "heloo..."
      -name: Print goodbye
      run: echo "Done - bye!"
